# SeldJEPA — CPU deps. Resolved/verified on Python 3.12, CPU. torch==2.8.0 baseline.
# Encoder (causal Conformer) / SIGReg (Epps-Pulley + slicing) / features
# (torch.stft + hand-rolled mel) / ADPIT loss are pure-torch + numpy.
# Install:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.8.0
#   pip install -r requirements.txt
torch==2.8.0
transformers==4.57.6
accelerate==1.13.0
numpy==2.4.6
PyYAML==6.0.3
scipy==1.17.1               # Hungarian matching in seld_metrics + dataset synthesis
soundfile==0.13.1           # 2-channel wav IO
pyroomacoustics==0.10.1     # only for make_seld_dataset.py (optional; free-field fallback)
