# V-JEPA 2.1 — CPU pre-training / eval deps.
# Resolved/verified on Python 3.12, CPU. torch==2.8.0 baseline.
# NOTE: this directory imports the encoder + trainer base from the sibling
# ../VJEPA2, so keep both directories under the same parent.
# Install:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.8.0
#   pip install -r requirements.txt
torch==2.8.0
transformers==4.57.6
accelerate==1.13.0
numpy==2.4.6
PyYAML==6.0.3
# Only needed to decode real .mp4/.webm clips (the synthetic smoke test
# uses .npy and does NOT need this):
decord==0.6.0
